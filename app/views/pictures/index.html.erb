
<% @pictures.each do |picture| %>
	<article class="index_picture">
	<div><%=link_to image_tag(picture.url_m) , picture.url , :rel=>'shadowbox[cat]'%></div>
	<header><%= link_to picture.flickr["title"], picture%> by <%= link_to picture.user.name, picture.user%></header>
	</article>
<% end %>

